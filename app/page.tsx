import Image from "next/image";
import LoginForm from "./components/LoginForm";

const Home: React.FC = () => {
  return (
    <div>
      <section
        style={{
          height: 200,
          background: "red",
          display: "flex",
          justifyContent: "center",
          alignItems: "center",
        }}
      >
        SECTION
      </section>
    </div>
  );
};

export default Home;
